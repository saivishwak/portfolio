import type { Config } from '@/types/data';
import { enUS } from 'date-fns/locale';
import type { ReadonlyDeep } from 'type-fest';

const config = {
  i18n: {
    locale: enUS,
    dateFormat: 'MMMM yyyy',
    translations: {
      now: 'now',
    },
  },
  meta: {
    title: 'Sai Vishwak K - Software Engineer',
    description:
      "I'm a Software Engineer at ServiceNow, specializing in Performance Engineering, Building scalable applications using my skills in Full-Stack Development, JavaScript, and Java",
    faviconPath: '/src/assets/ProfilePic.jpg',
  },
  pdf: {
    footer:
      'I hereby give consent for my personal data included in my application to be processed for the purposes of the recruitment process.',
  },
} as const satisfies ReadonlyDeep<Config>;

export default config;
